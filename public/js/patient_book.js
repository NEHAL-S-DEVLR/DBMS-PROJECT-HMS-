document.addEventListener("DOMContentLoaded",loadDoctors);async function loadDoctors(){status.textContent="Loading doctors...";doctorContainer.innerHTML="";try{const d=await getJSON("/loaddata/doctorlist");status.textContent=`Loaded ${d.length} doctor(s).`;d.forEach(x=>{let c=document.createElement("div");c.className="doctor-card filter-card";c.innerHTML=`<h3>${safe(x.name)}</h3><p>${badge("active")} ${safe(x.specialization)}</p><div><b>Email:</b> ${safe(x.email)}</div><div><b>Phone:</b> ${safe(x.phone_no)}</div>`;c.onclick=()=>{document.querySelectorAll(".doctor-card").forEach(a=>a.classList.remove("selected"));c.classList.add("selected");selectedDoctorId.value=x.user_id};doctorContainer.appendChild(c)})}catch(err){status.innerHTML=`<span class="error">${err.message}</span>`}}reqappointment.addEventListener("submit",async e=>{e.preventDefault();if(!selectedDoctorId.value){formMessage.innerHTML='<span class="error">Please select a doctor</span>';return}try{const d=await postJSON("/request/reqappointment",{doctor_id:selectedDoctorId.value,date:date.value,category:category.value,textInput:textInput.value});formMessage.innerHTML=`<span class="success">${d.message||"Appointment requested"}</span>`;reqappointment.reset();selectedDoctorId.value="";toast("Appointment requested")}catch(err){formMessage.innerHTML=`<span class="error">${err.message}</span>`}});