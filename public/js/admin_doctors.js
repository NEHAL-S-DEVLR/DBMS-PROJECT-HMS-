document.addEventListener("DOMContentLoaded",loadRecords);
async function loadRecords(){const status=document.getElementById("status"),container=document.getElementById("doctorContainer");status.textContent="Loading...";container.innerHTML="";try{const d=await getJSON("/loaddata/doctorlist");status.textContent=`Loaded ${d.length} record(s).`;d.forEach(x=>{let c=document.createElement("div");c.className="card filter-card";c.innerHTML=`<h3>${safe(x.name)}</h3><p>${badge('active')} ${safe(x.specialization)}</p><div><b>ID:</b> ${safe(x.user_id)}</div><div><b>Email:</b> ${safe(x.email)}</div><div><b>Phone:</b> ${safe(x.phone_no)}</div>`;container.appendChild(c);})}catch(err){status.innerHTML=`<span class="error">${err.message}</span>`}}